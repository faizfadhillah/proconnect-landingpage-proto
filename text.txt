import vuetify, { transformAssetUrls } from "vite-plugin-vuetify";
// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  compatibilityDate: "2024-04-03",
  devtools: { enabled: true },
  devServer: {
    port: 3011,
  },
  build: {
    transpile: ["vuetify"],
    //analyze: true,
  },
  css: ["@/assets/css/main.css"],
  modules: [
    (_options, nuxt) => {
      nuxt.hooks.hook("vite:extendConfig", (config) => {
        // @ts-expect-error
        config.plugins.push(vuetify({ autoImport: true }));
      });
    },
    "@pinia/nuxt",
    "@nuxtjs/google-fonts",
    //...
  ],
  //plugins: ["~/utils/auth.js"],
  googleFonts: {
    families: {
      Montserrat: [400, 600, 700], // Font weights for Montserrat
    },
  },
  vite: {
    ssr: {
      noExternal: ["vuetify"], // Treat Vuetify as an SSR-friendly dependency
    },
    vue: {
      template: {
        transformAssetUrls,
      },
    },
    build: {
      rollupOptions: {
        output: {
          manualChunks: {
            // Group Vuetify into a separate chunk
            vuetify: ["vuetify", "vuetify/components", "vuetify/directives"],
            // Group Material Design Icons into a separate chunk
          },
        },
      },
      // Increase chunk size warning limit (optional, if you want to suppress the warning)
      chunkSizeWarningLimit: 1000, // Set to 1000 kB
    },
  },
  runtimeConfig: {
    public: {
      FIREBASE_API_KEY: process.env.FIREBASE_API_KEY,
      FIREBASE_AUTH_DOMAIN: process.env.FIREBASE_AUTH_DOMAIN,
      FIREBASE_PROJECT_ID: process.env.FIREBASE_PROJECT_ID,
      FIREBASE_STORAGE_BUCKET: process.env.FIREBASE_STORAGE_BUCKET,
      FIREBASE_MESSAGING_SENDER_ID: process.env.FIREBASE_MESSAGING_SENDER_ID,
      FIREBASE_MESSAGING_VAPID_KEY: process.env.FIREBASE_MESSAGING_VAPID_KEY,
      FIREBASE_APP_ID: process.env.FIREBASE_APP_ID,
      FIREBASE_MEASURMENT_ID: process.env.FIREBASE_MEASUREMENT_ID,
      apiBase: process.env.API_BASE_URL || "http://localhost:3010",
    },
  },
  pinia: {
    storesDirs: ["./stores/**"],
  },
  components: true,
  /*routeRules: {
    // Semua admin routes
    "/admin/**": {
      ssr: false,
    },
  },*/
});
