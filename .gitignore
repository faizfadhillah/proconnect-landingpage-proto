# Real Firebase config — keep local, check firebase-config.example.js instead
assets/auth/firebase-config.js

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (if anything builds locally later)
node_modules/
npm-debug.log
yarn-error.log

# Netlify build output (generated by scripts/netlify-build.sh)
_site/
